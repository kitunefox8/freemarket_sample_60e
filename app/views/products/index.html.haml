.wrapper#__next
  = render "shared/header"
  .main__container
    .main__container__sliders
      .main__container__slider
        .main__container__slider__image
        .slider-control-centerleft
          .eoeUvk
        .slider-control-centerright
          .sc-VJcYb.sc-LKuAh.dkZuVW.sc-hmXxxW.cwGzbB
        .slider-control-bottomcenter
          .style_dotsIndicator__1fZnt.common_fontFamily__3-3Si.style_center__2IkR_.style_mono__3e12j.sc-iBEsjs.fJxojC
            %span.style_active__2ihlK
      .popular__categorys
        %h2.popular__category 人気のカテゴリー
        .popular__category__lists
          %ul.popular__category__lists__unordered
          - @categories.each do |category|
            %li.popular__category__list

              = link_to '#', class: "category" do
                = category[1]
      - @categories.each do |category|
        .new__product
          .ladies
            .ladies__new{name: "scroll-1"}
              %h3.style_heading.common_fontFamily__3-3Si.style_responsiveLv2__3K0hn.sc-chbbiW.kHlukY
                = "#{category[1]} 新着アイテム"
              = link_to "もっと見る", "#", class: "more_view"
            .ladies__items
              .ladies__item              
                - @ladies.each do |product|
                  - if product.category_id == category[0]                   
                    .product
                      =link_to product_path(product.id) do
                        = image_tag product.images[0].image_url.url
                      .name
                        =product.name
                      .price
                        =product.price                        
                      -if product.buyer == "1" 
                        .items-box_photo__sold
                          .items-box_photo__sold__inner
                            SOLD

                - @mans.each do |product|
                  - if product.category_id == category[0]                   
                    .product
                      =link_to product_path(product.id) do
                        = image_tag product.images[0].image_url.url
                      .name
                        =product.name
                      .price
                        =product.price                        
                      -if product.buyer == "1" 
                        .items-box_photo__sold
                          .items-box_photo__sold__inner
                            SOLD
                    
                - @kids.each do |product|
                  - if product.category_id == category[0]                  
                    .product
                      =link_to product_path(product.id) do
                        = image_tag product.images[0].image_url.url
                      .name
                        =product.name
                      .price
                        =product.price                        
                      -if product.buyer == "1" 
                        .items-box_photo__sold
                          .items-box_photo__sold__inner
                            SOLD

                - @interi.each do |product|
                  - if product.category_id == category[0]                   
                    .product
                      =link_to product_path(product.id) do
                        = image_tag product.images[0].image_url.url
                      .name
                        =product.name
                      .price
                        =product.price                        
                      -if product.buyer == "1" 
                        .items-box_photo__sold
                          .items-box_photo__sold__inner
                            SOLD

                - @book.each do |product|
                  - if product.category_id == category[0]                   
                    .product
                      =link_to product_path(product.id) do
                        = image_tag product.images[0].image_url.url
                      .name
                        =product.name
                      .price
                        =product.price                        
                      -if product.buyer == "1" 
                        .items-box_photo__sold
                          .items-box_photo__sold__inner
                            SOLD


      .popular__brands
        %h2.popular__brand 人気のブランド
        .popular__brand__lists
          %ul.popular__brand__lists__unordered
            %li.popular__brand__list
              %a.category シャネル
            %li.popular__brand__list
              %a.category ルイヴィトン
            %li.popular__brand__list
              %a.category シュプリーム
            %li.popular__brand__list
              %a.category ナイキ
      .new__product
        .chanel
          .chanel__new{name: "scroll-658"}
            %h3.style_heading.common_fontFamily__3-3Si.style_responsiveLv2__3K0hn.sc-chbbiW.kHlukY シャネル新着アイテム
            = link_to "もっと見る", "#", class: "more_view"
          .chanel__items
            .chanel__item
              - @product.each do |product|
                .product
                  =link_to product_path(product.id) do
                    = image_tag product.images[0].image_url.url
                  .name
                    = product.name
                  .price

                    = product.price            

                  -if product.buyer == "1"
                    .items-box_photo__sold
                      .items-box_photo__sold__inner 
                        SOLD

                  -elsif product.buyer == "2" 
                    .items-box_photo__sold
                      .items-box_photo__sold__inner
                        SOLD

  = render 'shared/footer'